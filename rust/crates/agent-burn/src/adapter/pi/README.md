# pi-agent Source

Data source:

```text
${PI_AGENT_DIR:-~/.pi/agent/sessions/}
```

Public CLI flow:

```sh
agent-burn summary
agent-burn summary --json
agent-burn summary --pi-path /path/to/sessions
```
