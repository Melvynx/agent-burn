{
	"combinedOptions": {
		"summary_options": ["shared_options", "summary_value_options"],
		"harness_options": ["shared_options", "harness_value_options"]
	},
	"root": {
		"usage": ["agent-burn [summary] [RANGE] <OPTIONS>", "agent-burn harness <codex|claude> <OPTIONS>"],
		"commands": [
			{
				"name": "summary",
				"description": "Compare API-equivalent agent usage with subscription value"
			},
			{
				"name": "harness",
				"description": "Inspect one subscription harness with live limits and value"
			}
		]
	},
	"pages": [
		{
			"path": ["summary"],
			"description": "Compare detected coding-agent usage, model spend, and subscription value. This is the default command when no command is provided.",
			"usage": "agent-burn summary [RANGE] <OPTIONS>",
			"options": "summary_options"
		},
		{
			"path": ["harness"],
			"description": "Focused weekly rate-limit detail for one harness: limit used vs time elapsed, value extrapolation, and a day-by-day breakdown.",
			"usage": "agent-burn harness <codex|claude> <OPTIONS>",
			"options": "harness_options"
		}
	]
}
